algoritimos e logica de programação.

input : são momentos de interação com usuário.
comandos de entrada: procura um pedido, solicita um pedido.


process : são verificações e ações para realizar
o pedido do usuário.

comandos de processamento:
verifica se o pagamento foi aprovado.
verifica se o restaurante está aberto.
começa a preparar seu pedido.

output:
são comandos de saida, para retornar alguma coisa ou dar algum posicionamento ou mensagem para o usuario.

comando de saída:
seu pedido está sendo levado para você.

A DIFERENÇA DE CONSTANTE E VARIAVEIS.
para declarar constante coloca " const" 
exemplo : const notificação = " pokemon GO diz: "

na VARIAVEL a função LET faz mudanças.
exemplo: let notificação



OPERADORES ARITMETICOS:
&& = adn logico = if ( condition && condition2)
|| = OR logico = if( condition1 || condition2)
! = NOT logico = if (!condition)

OPERADORES DE ATRIBUIÇÃO:
= atribuição = let x = 5;
+= adição e atribuição = let num = 10; num += 2;
-= subtração e atribuição = let num = 10; num -= 3;
